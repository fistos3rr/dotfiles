// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
      "alt f f": "file_finder::Toggle",
    },
  },
  {
    "context": "Editor || Workspace || EmptyPane || SharedScreen || Terminal",
    "bindings": {
      "F3": "project_panel::Toggle",
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      "ctrl-e": "editor::ShowCompletions"
    },
  },
  {
    "context": "Editor && showing_completions",
    "bindings": {
      "enter": "editor::Newline",
      "tab": "editor::ConfirmCompletion",
      "ctrl-e": "editor::Cancel",
      "escape": ["workspace::SendKeystrokes", "ctrl-e escape"],
    },
  },
  {
    "context": "Editor && (vim_mode == normal || vim_mode == visual) && !VimWaiting && !menu",
    "bindings": {
      //
    },
  },
  {
    "context": "Editor && vim_mode == normal && !VimWaiting && !menu",
    "bindings": {
      // Move between panes
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-j": "workspace::ActivatePaneDown",
      // LSP
      "alt c a": "editor::ToggleCodeActions",
      "alt k": "editor::ShowSignatureHelp",
      // Next/prev error
      "] e": ["editor::GoToDiagnostic", { "severity": "error" }],
      "[ e": ["editor::GoToPreviousDiagnostic", { "severity": "error" }],
      // Next/prev warning
      "] w": ["editor::GoToDiagnostic", { "severity": "warning" }],
      "[ w": ["editor::GoToPreviousDiagnostic", { "severity": "warning" }],
      // Next/prev hint
      "] H": ["editor::GoToDiagnostic", { "severity": "hint" }],
      "[ H": ["editor::GoToPreviousDiagnostic", { "severity": "hint" }],
      // Next/prev buffer
      "alt-.": "pane::ActivateNextItem",
      "alt-,": "pane::ActivatePreviousItem",
      // Swap tabs
      "alt->": "pane::SwapItemRight",
      "alt-<": "pane::SwapItemLeft",
      // Close active buffer
      "alt-c": "pane::CloseActiveItem",
    },
  },
  {
    "context": "EmptyPane || SharedScreen",
    "bindings": {

    },
  },
  // Toggle terminal
  {
    "context": "Workspace",
    "bindings": {
      "F7": "workspace::NewCenterTerminal",
    },
  },
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-j": "workspace::ActivatePaneDown",
      // Next/prev buffer
      "alt-.": "pane::ActivateNextItem",
      "alt-,": "pane::ActivatePreviousItem",
      // Close active buffer
      "alt-c": "pane::CloseActiveItem",
      // Swap tabs
      "alt->": "pane::SwapItemRight",
      "alt-<": "pane::SwapItemLeft",
    },
  },
  // File panel (netrw)
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "a": "project_panel::NewFile",
      "A": "project_panel::NewDirectory",
      "r": "project_panel::Rename",
      "d": "project_panel::Delete",
      "x": "project_panel::Cut",
      "c": "project_panel::Copy",
      "p": "project_panel::Paste",
      // Close project panel as project file panel on the right
      "q": "workspace::ToggleRightDock",
      "alt e": "workspace::ToggleRightDock",
      // Navigate between panel
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-j": "workspace::ActivatePaneDown",
    },
  },
  // Panel navigation
  {
    "context": "Dock",
    "bindings": {
      "ctrl-w h": "workspace::ActivatePaneLeft",
      "ctrl-w l": "workspace::ActivatePaneRight",
      "ctrl-w k": "workspace::ActivatePaneUp",
      "ctrl-w j": "workspace::ActivatePaneDown",
    },
  },
]
